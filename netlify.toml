[build]
  command = "corepack enable && pnpm install --no-frozen-lockfile && pnpm exec zenstack generate && pnpm exec prisma generate && pnpm run build"
  publish = "build/client"

[build.environment]
  NODE_VERSION = "20"
